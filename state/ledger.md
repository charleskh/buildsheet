# buildsheet ledger

Open loops only. Closed items move to `decisions.md`. Keep this an index, not a record.

## In flight

| Item | State |
|------|-------|
| Phases 1 through 6 of the build | Phase 0 gate passed 2026-09-24 |

## Awaiting a decision

| Item | Who |
|------|-----|
| Add `make.seethespecs.com` origin to STS `CORS_ALLOWED_ORIGINS` so import works | CEO, production secret |
| Make the repository public and push it to `charleskh/buildsheet` | CEO, outward facing |
| Default branch name at publish time. Local work is on `work`. | CEO |

## Loose ends

- Whether a claimed Cloudflare Drop deployment lands as Pages or Workers, and whether the same
  file limits apply. A five minute test, not a blocker.
